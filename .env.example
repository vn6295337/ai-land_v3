# AI Models Dashboard v3 - Frontend-Only Configuration
# For test deployments without database dependencies

NODE_ENV=production

# Optional: Analytics & Monitoring
VITE_APP_NAME=AI Models Discovery Dashboard v3
VITE_APP_VERSION=3.0.0

# Optional: External API endpoints (if needed)
# VITE_HUGGINGFACE_API_URL=https://huggingface.co/api/models
# VITE_OPENROUTER_API_URL=https://openrouter.ai/api/v1/models

# Note: No Supabase configuration needed for frontend-only deployment
# All data persisted using localStorage/sessionStorage