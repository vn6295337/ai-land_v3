name: Security Scan

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'

permissions:
  contents: read
  security-events: write

jobs:
  security-scan:
    name: Security Analysis
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run npm audit
      run: |
        echo "🔍 Running npm audit..."
        npm audit --audit-level=high

    - name: Check for security vulnerabilities in package-lock.json
      run: |
        echo "🔍 Checking package-lock.json for known vulnerabilities..."
        npm audit --audit-level=high --production

    - name: Scan for hardcoded secrets
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        extra_args: --debug --only-verified
      if: github.event_name == 'pull_request'

    - name: Run CodeQL Analysis
      uses: github/codeql-action/init@v3
      with:
        languages: javascript

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3

    - name: Check environment variable validation
      run: |
        echo "🔍 Testing environment validation..."
        # Test with missing env vars using root config (should fail)
        if NODE_ENV=production npx vite build 2>/dev/null; then
          echo "❌ Build should fail with missing environment variables"
          exit 1
        else
          echo "✅ Build correctly fails with missing environment variables"
        fi

    - name: Security scan summary
      run: |
        echo "🛡️ Security scan completed"
        echo "✅ NPM audit passed"
        echo "✅ Secret scanning completed"
        echo "✅ CodeQL analysis completed"
        echo "✅ Environment validation tested"